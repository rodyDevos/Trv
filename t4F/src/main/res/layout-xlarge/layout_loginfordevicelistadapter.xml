<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@color/white" 
    
    >
    <ViewStub
        android:id="@+id/fb_profile_pic_stub"
        android:layout_width="80dip"
        android:layout_height="80dip"
        android:inflatedId="@+id/fb_picker_image"
        android:layout="@layout/layout_fb_picker_image" />

    <TextView
        android:id="@+id/fb_picker_title"
        android:layout_width="200dip"
        android:layout_height="100dip"
        android:layout_weight="1.0"
        android:ellipsize="end"
        android:gravity="left|center_vertical"
        android:paddingLeft="10dip"
        android:singleLine="true"
        android:textColor="#000000"
        android:textSize="36sp"
        android:textStyle="bold" 
        android:clickable="true"
        android:onClick="onItemLongClick"/>

    <CheckBox
        android:id="@+id/deviceNotify"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginRight="20dip"
        android:onClick="onCheckBoxClicked"
        />

</LinearLayout>